{
  "name": "kurdish-imposter-server",
  "version": "0.1.0",
  "private": true,
  "description": "PartyKit server (one Durable Object per room) for Kurdish Imposter online multiplayer.",
  "scripts": {
    "dev": "partykit dev",
    "deploy": "partykit deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "partykit": "^0.0.111",
    "partyserver": "^0.0.66"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^1.6.0",
    "@types/node": "^20.12.7"
  }
}
